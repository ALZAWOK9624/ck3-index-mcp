// Package mcpserver exposes the ck3-index read-only MCP server.
//
// The package owns the MCP protocol envelope, stdio transport, tool catalog,
// dispatch compatibility layer, and result encoding. CK3 indexing and map
// behavior remain in internal/indexer.
package mcpserver
